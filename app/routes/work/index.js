import paths from '../paths';
import WorkRoute from './component';

export default {
  path: paths.WORK,
  exact: true,
  props: {
    // Props to pass down to component
  },
  component: WorkRoute
};
